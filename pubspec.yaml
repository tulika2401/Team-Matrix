name: grameen_finance_ai
description: "A rural-first multilingual finance app for farmers"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🌐 Backend & Networking
  http: ^1.1.0

  # 🎙 Audio (Text-to-Speech playback)
  audioplayers: ^5.2.1
  path_provider: ^2.1.0
  speech_to_text: ^6.6.0


  # 🧠 State & Storage
  provider: ^6.0.5
  shared_preferences: ^2.2.2

  # 📊 Charts (Diary)
  fl_chart: ^0.66.0

  # 🌍 Localization
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # 🎨 Icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
