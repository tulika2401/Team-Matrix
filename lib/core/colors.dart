import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Primary palette (Dark Green – Trust)
  static const Color primary = Color.fromARGB(255, 223, 168, 125);
  static const Color secondary = Color.fromARGB(255, 228, 205, 159);
  static const Color accent = Color(0xFFFFF3E0);

  // 💙 Trust & Info
  static const Color info = Color.fromARGB(255, 42, 127, 224);

  // ✅ Status colors
  static const Color success = Color(0xFF2E7D32);
  static const Color danger = Color(0xFFC62828);
  static const Color warning = Color(0xFFFB8C00);

  // 🧾 UI
  static const Color background = Color.fromARGB(255, 248, 243, 233);
  static const Color darkBackground = Color(0xFF121212);
  static const Color card = Colors.white;
}
